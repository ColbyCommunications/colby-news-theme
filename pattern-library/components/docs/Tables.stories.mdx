import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import Table from '../components/Table';
import WPCSS from '../components/content/WPThemeCSS';

<Meta title="Component Demos/Third-Party Blocks/Tables" />

# WordPress Tables

Table markup is generated by the WordPress "Table" block, and is not controlled directly
by the theme. The only control we have over tables is with CSS.

## Default Table Style
<Canvas>
<Story name='Default Table'>
    <div class="container mx-auto">
        <Table colCount={4} rowCount={4}></Table>
    </div>
</Story>
</Canvas>

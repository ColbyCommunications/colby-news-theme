<nav aria-label="Section Navigation" class="py-5 bg-blue-400 breadcrumbs">
    <div class="container mx-auto px-container">
        <ul class="flex space-x-8">
            {% for link in links %}
                <li><a class="text-lg font-bold text-primary-dark hocus:underline" href="{{ link.url }}">{{ link.title }}</a></li>
            {% endfor %}
        </ul>
    </div>
</nav>